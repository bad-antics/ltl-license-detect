// ltl-license-detect/main.ltl
//
// Heuristic SPDX license detector for source files.
//
// This is an early skeleton — contribution is welcome! See the
// README for build instructions.

import std::io
import std::fmt

pub fn main(args: [str]) -> Int {
    io::println("ltl-license-detect v0.1.0")
    io::println("Heuristic SPDX license detector for source files.")
    0
}
